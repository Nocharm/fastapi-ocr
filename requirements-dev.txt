# =====================================================================
# requirements-dev.txt — 개발/테스트 전용 의존성
#
# 프로덕션 런타임에는 필요 없는 도구들.
# Docker 이미지 빌드 시 이 파일은 포함하지 않는다.
#
# 설치 방법 (로컬 개발 환경):
#   pip install -r requirements-dev.txt
#
# -r requirements.txt : 프로덕션 의존성을 먼저 포함한다.
#   개발 환경은 프로덕션 패키지 + 아래 도구를 모두 설치한다.
# =====================================================================

-r requirements.txt

# --- 테스트 ---
pytest==8.3.2
httpx==0.27.2               # FastAPI TestClient 내부 의존성
