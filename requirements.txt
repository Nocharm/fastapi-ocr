# =====================================================================
# 설치 순서 주의:
#   easyocr는 내부적으로 torch/torchvision을 의존하는데,
#   PyPI 기본 인덱스에서 Linux용 torch를 받으면 CUDA 버전(~2 GB)이 설치된다.
#   Docker 빌드 시 requirements.txt 전에 아래 명령어로 CPU 전용 버전을 먼저 설치한다:
#
#     pip install torch torchvision \
#         --index-url https://download.pytorch.org/whl/cpu
#
#   Dockerfile에 이미 적용되어 있으므로 로컬 개발 환경에서는 별도 조치 불필요.
# =====================================================================

# --- 웹 프레임워크 ---
fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9     # UploadFile form-data 파싱에 필요
pydantic-settings==2.3.4

# --- PDF 처리 ---
pdfplumber==0.11.4           # 텍스트 레이어 직접 추출
pdf2image==1.17.0            # PDF → 이미지 변환 (시스템에 poppler 필요)

# --- 이미지 처리 ---
opencv-python-headless==4.10.0.84   # GUI 없는 서버 환경용 OpenCV
Pillow==10.4.0
numpy==1.26.4               # numpy 2.x는 현재 스택과 호환 안 됨 — 1.x 고정

# --- OCR 엔진 ---
# torch/torchvision은 Dockerfile에서 CPU 전용으로 먼저 설치됨 (위 주석 참고)
torch==2.2.2
torchvision==0.17.2
easyocr==1.7.1
pytesseract==0.3.13

# --- 데이터 처리 ---
pandas==2.2.2

# --- 테스트 (프로덕션 빌드에서는 불필요) ---
pytest==8.3.2
httpx==0.27.2               # FastAPI TestClient 내부 의존성
