# =====================================================================
# requirements.txt — 프로덕션 런타임 의존성
#
# 개발/테스트 전용 패키지(pytest 등)는 requirements-dev.txt에 관리한다.
# Docker 이미지는 이 파일만 설치한다.
#
# [변경 이력]
#   - EasyOCR 제거: easyocr, torch, torchvision 및 전용 의존성 삭제.
#     torch/torchvision은 수 GB 규모여서 이미지 빌드 시간의 주원인이었음.
#     EasyOCR이 사용하던 numpy, opencv, Pillow는 PDF 파이프라인에서
#     여전히 필요하므로 유지한다.
# =====================================================================

# --- 웹 프레임워크 ---
fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9     # UploadFile form-data 파싱에 필요
pydantic-settings==2.3.4

# --- PDF 처리 ---
pdfplumber==0.11.4           # 텍스트 레이어 직접 추출
pdf2image==1.17.0            # PDF → 이미지 변환 (시스템에 poppler 필요)

# --- 이미지 처리 ---
# EasyOCR은 제거되었지만, 아래 패키지는 extractor.py의 PDF OCR 파이프라인에서 사용한다.
#   opencv: Laplacian 선명도 평가, 이미지 전처리 (cv2)
#   Pillow: pdf2image가 PIL Image 객체를 반환하므로 필요
#   numpy : OpenCV / 이미지 배열 연산에 필요
opencv-python-headless==4.10.0.84   # GUI 없는 서버 환경용 OpenCV
Pillow==10.4.0
numpy==1.26.4               # numpy 2.x는 현재 스택과 호환 안 됨 — 1.x 고정

# --- OCR 엔진 (Tesseract only) ---
# torch / torchvision / easyocr 제거됨.
# Tesseract 바이너리는 Dockerfile에서 apt-get으로 설치된다.
pytesseract==0.3.13

# --- 데이터 처리 ---
pandas==2.2.2                # Tesseract image_to_data() 출력 처리에 사용

# 테스트 의존성은 requirements-dev.txt 에서 관리한다.
# 프로덕션 이미지에는 pytest / httpx 를 포함하지 않는다.
